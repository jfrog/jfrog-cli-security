yarnPath: .yarn/releases/yarn-4.5.0.cjs

nodeLinker: node-modules

# Allow the plain-HTTP httptest mock registry used by the curation integration test.
# Yarn Berry rejects http registries unless the host is whitelisted here.
unsafeHttpWhitelist:
  - "127.0.0.1"
  - "localhost"
