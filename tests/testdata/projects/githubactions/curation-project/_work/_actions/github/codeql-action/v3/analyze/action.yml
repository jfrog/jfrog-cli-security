name: 'CodeQL: analyze'
description: 'Composite action bundling the analyze subpath'
runs:
  using: 'composite'
  steps:
    - uses: some-org/transitive-action@v1
    - run: echo "analyze"
      shell: bash
